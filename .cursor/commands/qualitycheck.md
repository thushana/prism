Run "npm run quality" which typechecks, lints and formats
Review the errors (typecheck and lint may require manual fixes; format:fix auto-fixes formatting)
Iteratively address errors and re-run until all checks pass
Our goal is to get the code base into a commitable, deployable state.
