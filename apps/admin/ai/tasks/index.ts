/**
 * Admin App AI Tasks
 * Register all admin app-specific AI tasks here
 */

// Import and register your tasks here
// Example:
// import { MyAdminTask } from "./my-admin-task";
// import { TaskRegistry } from "intelligence/tasks/registry";
// TaskRegistry.register(new MyAdminTask());

// Export tasks for direct use
// Example:
// export * from "./my-admin-task";

// This file is the central registry for all admin app tasks
// Each task should be in its own directory with:
// - index.ts (task implementation)
// - prompt.ts (versioned prompts)
// - types.ts (Zod schemas and types)
