/** Prism-standard config (`config.prism.json`) — validated via application-settings. */
export {
  prismConfigBaseSchema as prismConfigSchema,
  type PrismConfigBase as PrismConfig,
} from "application-settings/prism-config-schema";
