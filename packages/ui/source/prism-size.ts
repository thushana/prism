/**
 * Shared named size scale for Prism UI (button, typography, icon named steps).
 * Do not redeclare this union on components — import `PrismSize` from here.
 */
export type PrismSize = "small" | "medium" | "large" | "huge" | "gigantic";
